import { defineConfig } from "@lovable.dev/vite-tanstack-config";

export default defineConfig({
  // We leave this empty. The Lovable config is smart enough 
  // to build for Vercel if we don't confuse it with Cloudflare files.
});